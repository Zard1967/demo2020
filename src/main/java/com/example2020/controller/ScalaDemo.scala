package com.example2020.controller

/**
 * 这是1个Scala的文档注释
 */
object ScalaDemo {
  def main(args: Array[String]): Unit = {
    var arr = new Array[Int](10)
    for (item <- arr) {
      println(item)
    }
  }
}
